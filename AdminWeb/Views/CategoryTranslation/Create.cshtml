@model AdminWeb.Models.CategoryTranslation
@{
    var currentArea = ViewContext.RouteData.Values["area"]?.ToString() ?? "";
    ViewData["Title"] = "Thêm bản dịch Danh mục";
    var languages = ViewBag.Languages as IEnumerable<AdminWeb.Models.SupportedLanguage>
        ?? Enumerable.Empty<AdminWeb.Models.SupportedLanguage>();
}

<div class="page-title">Thêm bản dịch Danh mục</div>
<div class="page-subtitle">Thêm nội dung và ngôn ngữ mới cho Danh mục.</div>

<div class="card" style="padding:28px;">
    <form asp-area="@currentArea" asp-action="Create" method="post">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="CategoryId" />

        <div class="form-grid">
            <div class="form-group">
                <label class="form-label">Mã ngôn ngữ</label>
                <select class="form-control" asp-for="LanguageCode" required>
                    <option value="">-- Chọn ngôn ngữ --</option>
                    @foreach (var language in languages)
                    {
                        <option value="@language.LanguageCode">@language.LanguageName (@language.LanguageCode)</option>
                    }
                </select>
                <small style="color:var(--text2);font-size:12px;margin-top:6px;display:block;">
                    Danh sách này lấy tự động từ Ngôn ngữ & giọng đọc.
                </small>
            </div>

            <div class="form-group full">
                <label class="form-label">Tên Danh mục</label>
                <input class="form-control" asp-for="Name" type="text" required />
            </div>
        </div>

        <div style="display:flex;gap:12px;flex-wrap:wrap; margin-top:24px;">
            <button type="submit" class="btn btn-primary"><i class="fa-solid fa-floppy-disk"></i> Lưu bản dịch</button>
            <a asp-area="@currentArea" asp-action="Index" asp-route-categoryId="@Model.CategoryId" class="btn btn-secondary">Quay lại</a>
        </div>
    </form>
</div>
